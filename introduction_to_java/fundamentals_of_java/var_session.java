class var_session
{
 public static void main(String[] args) {
    // System.out.println("1"); //everything inside double quotes is string
    // System.out.println(1+1); //everything not in double quotes is not a string 
   
    int x = 20,y=10; //both declare and initilaise multiple variables in one line 
    int z; //declaration of var
    z = 20; //initialization of var
    x=30;
    System.out.println(x+y+z);

    //naming convention in java
    //name of var should be related to data that it store
    //it cannot start with number a
    //case sensetive
    //can contain letter(Aa -zZ) and numbers(0-9)
    //no special char allowed except '_'underscore and '$' dollor
    //variable cant be declared twice
    //we cant use reserve keywords
    //variable name should be on left hand side of assighment operator


    
 }
}